/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com', 'raw.githubusercontent.com', 'fo4.garena.vn', 'dc.fconline.garena.vn'],
  },
};

export default nextConfig;